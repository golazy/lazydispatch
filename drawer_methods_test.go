package lazydispatch

/*
func TestDrawer_Get(t *testing.T) {

	d := NewScope()
	h := newFakeHandler("test")

	d.Get("/", h)

	for _, route := range d.routes() {
		if isHandler(route.Handler, "test") != true {
			t.Error(route)

		}
	}

}


*/
