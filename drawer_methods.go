package lazydispatch

/*
func (r *Drawer) Get(p string, handler http.Handler) {
	if r.method != "" && r.method != "GET" {
		panic("Method mismatch")
	}
	r.Add(&route.Route{
		Method:  "GET",
		URL:     path.Join(r.path, p),
		Handler: handler,
	})
}

*/
